build:
  docker:
    web: Dockerfile
    worker:
      dockerfile: Dockerfile
      target: builder 
release:
  image: worker
  command:
    - cd /app/db && make install && make migrate/up
run:
  web: /main
